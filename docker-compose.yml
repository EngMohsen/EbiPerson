version: '3.7'
services:
  app-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8000:8000'
    restart: always
    networks:
      - backend
networks:
  backend: null